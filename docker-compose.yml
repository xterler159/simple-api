version: '3'
services:
  database:
    image: "postgres"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
    ports:
      - "6000:5432" #HOST:CONTAINER
